The syntax of a programming lan is the fomr of its expressions, statments and program units. It's semantics is the meaning of thoes expression, statments and programming units. 

EG in C a if statement is 

```
	if (<expr>) <statment
```

semantics should come from / follow on from the syntax of a well defined programming language. 

### the problems with describing syntax

Both natual lang and artifical langs are made of sets of characters. we call these sets sentences or statements. 

the smallest details of a lang are not often defined by the syntax are instead called a lexemes. the description of a lexemes can be given in a lexical specification. This is normally separate  from the syntactical description of a language. 

lexemes include
	identifiers
	literals
	operators 
	special words 

A program is just a string of lexemes. 


A token of a lang is a category of its lexemes. 
	E.G. A identifier is a token that can have lexemes or instances, such as sum or total.  In some instance a 